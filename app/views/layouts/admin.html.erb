<!DOCTYPE html>
<html>
<head>
  <title>Admin - gamer-bosyu-kaijo</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>
<body>

  <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    
        <div class="container">
          <a class ="navbar-brand" href="/admin/dashboards">
           <span>"ゲーマー募集会所"admin</span>
          </a>
          <%# ドロップダウンを設定 %>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
           </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
          
            <ul class="navbar-nav ml-auto"> 

              <% if admin_signed_in? %> 
                <li>
                  <a class="nav-link text-light" href="<%= admin_users_path %>" >
                   <i class="fa-solid fa-house"></i>
                    ユーザー管理
                  </a>
                </li>

                <li>
                  <a class="nav-link text-light" href="<%= admin_groups_path %>" >
                    <i class="fa-solid fa-house"></i>
                    グループ管理
                  </a>
                </li>

                <li>
                  <a class="nav-link text-light" href="<%= admin_posts_path %>">
                    <i class="fa-solid fa-house"></i>
                    投稿管理
                  </a>
                </li>

                
                <li>
                 <%= link_to  destroy_admin_session_path, method: :delete, class:"nav-link text-light" do%>
                  <i class="fa-solid fa-right-from-bracket"></i>
                  ログアウト
                  <%end%>
                </li>

              <%else%>
                <li>
                  <a class="nav-link text-light" href="<%= new_admin_session_path %>" >
                    <i class="fa-solid fa-house"></i>
                   ログイン
                  </a>
                </li>
              <%end%>

            </ul>
          </div>
        </div>
        
      </nav>
    </header>
    
    <%# フラッシュメッセージ表示 %>
    <div id="flash-messages" class="min-vh-10 d-flex align-items-center justify-content-center">
      <p class="text-success font-weight-bold"><%= flash[:notice] %></p>
      <p class="text-danger font-weight-bold"><%= flash[:alert] %></p>
    </div>
    
  <%= yield %>
</body>
</html>